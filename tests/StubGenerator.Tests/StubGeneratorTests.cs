using NUnit.Framework;


namespace StubGenerator.Tests;

public class StubGeneratorTests
{
    [SetUp]
    public void Setup()
    {
    }

    [Test]
    public void Test1()
    {
        var input = "input v:string.256";

        //var parsedInputResult = InputParser.Parse(input);
        // var parsedOutputResult = StubOutputParser.Parse(parsedInputResult.Statements, Language.Csharp);


        //       Assert.IsTrue(result.IsSuccess);
    }
}
